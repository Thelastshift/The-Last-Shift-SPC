# The Last Shift

GitHub Pages package.

## Upload
Upload `index.html` and the entire `images` folder to the root of your GitHub repository.

Then open:
Settings → Pages → Deploy from a branch → main → /(root) → Save
